import { Arvo, Inter, Lusitana, Courier_Prime } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({weight: '400', subsets: ['latin']});
export const courier_prime = Courier_Prime({weight: '400', subsets: ['latin']});
export const arvo = Arvo({weight: '400', subsets: ['latin']});